{
  "name": "career-canvas-backend",
  "version": "1.0.0",
  "description": "Backend API for Career Canvas application",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx tsc",
    "watch": "npx tsc -w",
    "start": "npm run build && func start",
    "start:dev": "func start --typescript",
    "start:func": "npm run build && func start",
    "dev": "npm run build && func start --typescript",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "@azure/cosmos": "^4.0.0",
    "@azure/functions": "^4.0.0",
    "axios": "^1.6.0",
    "mongodb": "^6.19.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.13",
    "typescript": "^5.0.0"
  },
  "keywords": [
    "azure-functions",
    "career-canvas",
    "api"
  ],
  "author": "",
  "license": "ISC"
}
